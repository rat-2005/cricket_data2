Even ever i say a problem,try to solve slowly without jumping to conclusions.
Just pull out few examples from internet and compare with table results
If i say a problem, dont just think to solve only that problem, think about the logic and consequences of the changes and solve the whole problem
for every query, write a code in such a way it reads all the data from both cricksheet and cricinfo and remove the duplicates using match id, both share a same match id, why i am saying this because cricksheet has somedata which cricinfo doesnt have and vice versa, so to get the complete data, we need to consider both